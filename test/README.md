# Test / demo app moved to `demo/`

The runnable **demo and test app** now lives in the **[demo/](../demo/)** folder, next to the library and delivery packages.

- **Run the demo:** from repo root, `npm run build:demo` then `npm run demo`; open http://localhost:3000.
- **Full instructions:** [demo/README.md](../demo/README.md).

This `test/` folder is kept only for compatibility; all demo and test assets are in `demo/`.
